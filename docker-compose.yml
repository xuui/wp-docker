version: '2'
services:
  nginx:
    image: nginx:1.13.12
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./etc/nginx:/etc/nginx
      - ./logs/nginx:/var/log/nginx
      - ./htmls:/var/www/html
    restart: always

